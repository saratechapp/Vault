# Supabase project URL and anon (public) key, from Project Settings → API.
# Used only for Supabase Auth (signup/login/session refresh) — all other data
# access goes through the Express backend, which uses the service-role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
