# Copy to .env and fill in ONE provider key (matches the ChatXxx class you use in the script).
# .env / .env.* are already gitignored at the repo root.

ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GOOGLE_API_KEY=
# GROQ_API_KEY=

# Only for Browser Use Cloud browsers / ChatBrowserUse (optional):
# BROWSER_USE_API_KEY=
